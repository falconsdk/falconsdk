# @falcon/smart-accounts

The smart accounts sdk for falcon.

## Install

Install the package via `npm`:

```sh
npm install @falcon/smart-accounts
```

or `yarn`:

```sh
yarn add @falcon/smart-accounts
```
